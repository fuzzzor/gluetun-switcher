# Ignore the local WireGuard configuration folder
wireguard-configs-local/
*.docx
# Local environment file
.env
# Local configuration files and folders
locations.local.json
config-local/
# Dependencies
node_modules/
npm-debug.log*
# Local docker-compose (may contain credentials / personal config)
docker-compose.yml